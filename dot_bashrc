# .bashrc

if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]; then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
export PATH

if [ -d ~/.bashrc.d ]; then
    for rc in ~/.bashrc.d/*; do
        if [ -f "$rc" ]; then
            . "$rc"
        fi
    done
fi
unset rc

if command -v zoxide >/dev/null 2>&1; then
    eval "$(zoxide init --cmd cd bash)"
fi

if command -v fzf >/dev/null 2>&1; then
    if fzf --bash >/dev/null 2>&1; then
        eval "$(fzf --bash)"
    else
        [ -f /usr/share/doc/fzf/examples/key-bindings.bash ] && . /usr/share/doc/fzf/examples/key-bindings.bash
        [ -f /usr/share/doc/fzf/examples/completion.bash ] && . /usr/share/doc/fzf/examples/completion.bash
    fi
fi

if [ -d /usr/local/cuda-12.3/bin ]; then
    export PATH="/usr/local/cuda-12.3/bin:$PATH"
fi

if [ -d /usr/local/cuda-12.3/lib64 ]; then
    export LD_LIBRARY_PATH="/usr/local/cuda-12.3/lib64:${LD_LIBRARY_PATH:-}"
fi

if [ -d "$HOME/.lmstudio/bin" ]; then
    export PATH="$PATH:$HOME/.lmstudio/bin"
fi

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
# __conda_setup="$('/home/jkondratenko/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
# if [ $? -eq 0 ]; then
#     eval "$__conda_setup"
# else
#     if [ -f "/home/jkondratenko/anaconda3/etc/profile.d/conda.sh" ]; then
#         . "/home/jkondratenko/anaconda3/etc/profile.d/conda.sh"
#     else
#         export PATH="/home/jkondratenko/anaconda3/bin:$PATH"
#     fi
# fi
# unset __conda_setup

# Added by Antigravity CLI installer
export PATH="/home/jkondratenko/.local/bin:$PATH"

# Added by LM Studio CLI (lms)
export PATH="$PATH:/home/jkondratenko/.lmstudio/bin"
# End of LM Studio CLI section

